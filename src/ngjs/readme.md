### Description
    @angular/cli but for angularjs

### Options
```bash
Usage: 
    ngjs [command] [...options]

Command:
    (g   | generate)
Options:
    (a   | api)         -v [ version = default(1) ]
    (c   | component)
    (d   | directive)
    (s   | service)
    (data)
```
